# define your env variables for the test env here
APP_ENV=test
KERNEL_CLASS='App\Kernel'
APP_SECRET='$ecretf0rt3st'
# JWT_PASSPHRASE must be set in .env.test.local (not committed) or as a CI environment variable
