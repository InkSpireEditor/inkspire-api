framework:
    rate_limiter:
        ollama_generate:
            policy: sliding_window
            limit: 20
            interval: '1 minute'

when@test:
    framework:
        rate_limiter:
            ollama_generate:
                policy: no_limit
