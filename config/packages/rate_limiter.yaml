framework:
    rate_limiter:
        llm_generate:
            policy: sliding_window
            limit: 20
            interval: '1 minute'

when@test:
    framework:
        rate_limiter:
            llm_generate:
                policy: no_limit
